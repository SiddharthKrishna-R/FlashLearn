<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.flashlearn">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlashLearn"
        tools:targetApi="31">

        <!-- Declare the MainActivity as the launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declare the HomeScreenActivity -->
        <activity
            android:name=".HomeScreenActivity"
            android:exported="true" />

        <!-- Declare the FlashcardCreationActivity -->
        <activity
            android:name=".FlashcardCreationActivity"
            android:exported="true" />

        <!-- Declare the FlashcardDetailsActivity -->
        <activity
            android:name=".FlashcardDetailsActivity"
            android:exported="true" />

    </application>
</manifest>
